#include <stdio.h>
#include <stdlib.h>
#include <string.h>

typedef struct{
    struct Vert *cima;
    struct Vert *baixo;
    struct Vert *direita;
    struct Vert *esquerda;
    int lmax;
}Vert;


void setag(char **lab, int m, int n){
int i,j;
    Vert **grafo;

     grafo=(Vert**)malloc(n*sizeof(Vert*));

    for(i=0;i<m;i++){
        grafo[i]=(Vert*)malloc(sizeof(Vert));
    }
    for(i=0;i<n;i++){
              for(j=0;j<m;j++){
                grafo[i]->baixo= NULL;
                grafo[i]->cima=NULL;
                grafo[i]->direita=NULL;
                grafo[i]->esquerda=NULL;
              }
    }

   for(i=0;i<n;i++){
        for(j=0;j<m;j++){
            if(lab[i][j+1]!='#' && j+1<m){

            }
        }
    }
    for(i=0;i<n;i++){
        for(j=0;j<m;j++){

         // printf("%c",lab[i][j]);
            if(grafo[i][j-1].direita !=NULL){
                printf("-");
            }/*
        }
        if(grafo[i-1][j].baixo != NULL && grafo[i][j].cima!= NULL ){
                printf("|");
*/            }

        printf("\n");
    }
    printf("\n\n");

}

int main(){
    char **lab;
    int m,n,i,j;
    Vert *ant,*aux1;
    scanf("%d %d", &n,&m);
    //restricao


    lab=(char**)malloc(n*sizeof(char*));
    for(i=0;i<m;i++){
        lab[i]=(char*)malloc(sizeof(char));
    }




    for(i=0;i<n;i++){
        for(j=0;j<m;j++){
            scanf("%s",&lab[i][j]);
        }
    }


    setag(lab, m,n);


    for(i=0;i<n;i++){
        for(j=0;j<m;j++){

            printf("%c",lab[i][j]);
        }
        printf("\n");
    }

}
